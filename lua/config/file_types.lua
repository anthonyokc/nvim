-- file_types.lua: File type-specific settings
-- This is different than config/languages which is for language-specific settings
vim.filetype.add({
    extension = {
        templ = 'templ',
    }
})

